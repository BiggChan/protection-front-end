import React from 'react'
import Layout from '../../components/core/Layout'
import SwapToken from '../../components/core/SwapToken'
import SwapTable from '../../components/core/SwapTable'

const Swap = () => {
  return (
    <Layout>
      <SwapToken />
      <SwapTable />
    </Layout>
  )
}

export default Swap
